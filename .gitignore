# Specs library — separate PRIVATE repo. This repo is public; never track it here.
specs/

node_modules/
dist/
.astro/
TODO.md
.DS_Store

# Never commit — the key must not enter this public repo's history
.env

# Personal/financial documents. This repo is public — these must never be tracked.
# Move them out of the working tree entirely when convenient.
*.pdf
Screenshot*.png
DeJonghe_*
